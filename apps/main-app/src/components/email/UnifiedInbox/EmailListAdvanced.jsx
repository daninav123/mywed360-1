// Wrapper temporal para mantener compatibilidad mientras eliminamos el duplicado.
// Reexporta la implementación existente desde EmailList.jsx pero con un nombre
// más claro, evitando conflictos con la versión genérica del proyecto.

import EmailList from './EmailList';

export default EmailList;
