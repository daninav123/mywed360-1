/**
 * Hook DISABLED - No longer in use
 * Placeholder for backwards compatibility
 */
export const useSeatingPlan = () => {
  return {
    // Return minimal stub
    guests: [],
    tables: [],
    loading: false,
  };
};

export default useSeatingPlan;
